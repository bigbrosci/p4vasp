# p4vasp fixes — 2026-09-24

This update records the removal of the left-panel New button and the backup of its related code.

## Fixed issues

- Removed the **New** button from the left vertical toolbar in `data/glade2/frame.glade` at the user's request. **Open** is now the first toolbar button.
- Created `deprecated/new-button/` and saved unmodified copies of the original `data/glade2/frame.glade`, `data/glade2/new.glade`, and `lib/p4vasp/applet/NewApplet.py`, preserving their relative paths. The exact removed toolbar block is saved separately as `toolbar-button.glade.txt`.
- Added `deprecated/README.md` to record the reason, affected files, scope, and restoration instructions. Backup files are reference copies and are not loaded by the application.
- The **Edit → New** menu entry and New applet remain available. The change removes only the left-panel shortcut; the shared toolbar handler and other toolbar buttons are unchanged.

## Verification

- Verified that all three backed-up source files match their original Git versions byte for byte.
- Parsed the updated Glade XML successfully and compared its structure with the backup: the only change is removal of the New toolbar child. Confirmed that **Open** is the first remaining toolbar button.
- `git diff --check` passed. No GUI runtime test was performed for this change.

## Launch command

Close the existing application and run the updated checkout to load the modified toolbar:

```bash
cd /home/lqlhz/opt/p4vasp
PYTHONPATH="$PWD/lib:$PWD/src" P4VASP_HOME="$PWD" /usr/bin/python3 p4v.py
```

# p4vasp 修复记录 — 2026-09-24

本文记录左侧面板 New 按钮的移除及相关代码的备份。

## 已修复的问题

- 按用户要求，移除 `data/glade2/frame.glade` 中左侧垂直工具栏的 **New** 按钮。**Open** 现为工具栏的第一个按钮。
- 创建 `deprecated/new-button/`，保存修改前的 `data/glade2/frame.glade`、`data/glade2/new.glade` 和 `lib/p4vasp/applet/NewApplet.py` 完整副本，并保留原有相对路径。被移除的工具栏代码块单独保存为 `toolbar-button.glade.txt`。
- 新增 `deprecated/README.md`，记录移除原因、涉及文件、修改范围及恢复方法。备份文件仅供参考，不会被应用加载。
- 保留 **Edit → New** 菜单入口及 New 小程序。本次仅移除左侧面板的快捷按钮，共用工具栏事件处理逻辑和其他工具栏按钮保持不变。

## 验证结果

- 已逐字节确认三个源文件的备份均与修改前的 Git 版本一致。
- 修改后的 Glade XML 解析成功；与备份的结构比较确认，唯一变化是移除 New 工具栏子节点。已确认 **Open** 为剩余工具栏的第一个按钮。
- `git diff --check` 通过。本次修改未进行 GUI 运行测试。

## 启动命令

关闭已运行的应用后，从当前项目目录重新启动，以加载修改后的工具栏：

```bash
cd /home/lqlhz/opt/p4vasp
PYTHONPATH="$PWD/lib:$PWD/src" P4VASP_HOME="$PWD" /usr/bin/python3 p4v.py
```
