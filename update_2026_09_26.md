# p4vasp update — 2026-09-26

## English

### INCAR panel description

Shortened the help text shown in the **INCAR** panel to clarify that the open POSCAR is read and MAGMOM/UJ values use the defaults in `data/incar/presets.json`.

Previous text:

> MAGMOM reads the open POSCAR and starts each moment at zero. Edit values using Custom parameters.

Updated text:

> Reads the open POSCAR; MAGMOM/UJ values use data/incar/presets.json.

The new wording fits the panel's limited space and identifies where the defaults are configured. This change updates only the description in `lib/p4vasp/applet/IncarApplet.py`; it does not modify the presets or INCAR generation logic.

### Verification

- The focused GTK startup check for `p4vasp.applet.IncarApplet.IncarApplet` passed.
- `git diff --check` passed.
- The rendered text layout was not separately inspected, and the full test suite was not rerun for this wording change.

## 中文

### INCAR 面板说明文字

精简了 **INCAR** 面板中的提示文字，明确说明程序读取已打开的 POSCAR，并根据 `data/incar/presets.json` 中的默认设置确定 MAGMOM/UJ 参数值。

原界面文字：

> MAGMOM reads the open POSCAR and starts each moment at zero. Edit values using Custom parameters.

更新后的界面文字：

> Reads the open POSCAR; MAGMOM/UJ values use data/incar/presets.json.

中文含义：读取已打开的 POSCAR；MAGMOM/UJ 参数值采用 `data/incar/presets.json` 中的设置。

新说明更简短，适合面板的有限空间，同时指出默认值的配置文件位置。本次仅修改 `lib/p4vasp/applet/IncarApplet.py` 中的说明文字，未修改预设数据或 INCAR 生成逻辑。

### 验证结果

- `p4vasp.applet.IncarApplet.IncarApplet` 的 GTK 专项启动检查通过。
- `git diff --check` 通过。
- 未单独检查文字的实际显示布局；本次文字调整未重新运行完整测试套件。
